{
  "name": "file-handling-using-s3",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "setup": "npm install && concurrently npm:install:*",
    "install:client": "npm --prefix ./client install",
    "install:infrastructure": "npm --prefix ./infrastructure install",
    "start": "npm --prefix ./client run dev",
    "synth": "npm --prefix ./infrastructure run synth",
    "deploy": "npm --prefix ./infrastructure run deploy",
    "destroy": "npm --prefix ./infrastructure run destroy"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.1",
    "prettier": "^3.0.3",
    "prettier-plugin-svelte": "^3.0.3",
    "prettier-plugin-tailwindcss": "^0.5.4"
  },
  "dependencies": {
    "@libsql/client": "^0.3.4"
  }
}
